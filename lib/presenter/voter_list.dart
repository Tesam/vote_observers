import 'package:flutter/material.dart';

class VoterList extends StatelessWidget {
  const VoterList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Voter list"),
    );
  }
}
